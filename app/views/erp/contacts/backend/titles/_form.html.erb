<%= form_for([erp_contacts, :backend, title]) do |f| %>    
    <%= erp_form_control("text", {
        name: 'title[title]',
        value: title.title,                   
        label: t('contacts.titles.title'),
        required: true,
        errors: title.errors.full_messages_for(:title)
    }) %>
    <%= erp_form_control("text", {
        name: 'title[abbreviation]',
        value: title.abbreviation,                   
        label: t('contacts.titles.abbreviation'),
    }) %>
    <hr>
    <%= erp_component('button/save', {
        text: t('contacts.titles.submit'),
        class: ''
    }) %>
    <%= erp_component('button/cancel', {
        text: t('contacts.titles.cancel'),
        href: erp_contacts.backend_titles_path,
        class: ''
    }) %>
<% end %>